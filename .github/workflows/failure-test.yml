name: Failure Test

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Step 1
        run: echo "Step 1 succeeded"

      - name: Step 2
        run: exit 1

      - name: Step 3
        run: echo "Will this run?"
